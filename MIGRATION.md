# Migration Notes (Silent's LunarVim fork)

## Recommended environment baseline (Silent fork)

This fork targets Neovim `>= 0.11` and uses `lazy.nvim` with the effective lockfile at:
- `~/.config/lvim/lazy-lock.json`

### Neovim (required)
Install Neovim via Homebrew:

```bash
brew install neovim
nvim --version | head -n 2

## Recommended environment baseline (Silent fork)

See `docs/ENVIRONMENT.md` for recommended runtime environment and provider setup (node/python providers, optional provider disables, and exporting `:checkhealth`).

## Unreleased
- (TBD)

